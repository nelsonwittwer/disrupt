
<%= form_for [@startup, @discussion] do |f| %>
  <% if @discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :title, class: "span7"%>
  </div>
  <div class="actions">
    <br /><%= f.submit %>
  </div>
<% end %>
