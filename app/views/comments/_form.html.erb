
<%= simple_form_for [@startup, @discussion, @comment] do |f| %>
  <%= f.input :text %>
  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :parent_id %>

  <div class="actions">
    <br /><%= f.submit %>
  </div>
<% end %>
